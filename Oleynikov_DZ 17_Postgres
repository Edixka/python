-- SELECT TOP 1 [Код заказа], СуммаЗаказа FROM
-- (SELECT [Опиcания заказов__].[Код заказа], SUM([Опиcания заказов__].Количеcтво*Наборы___.Цена)
--  AS СуммаЗаказа
-- FROM Наборы___ INNER JOIN [Опиcания заказов__] ON Наборы___.[Код набора] = [Опиcания заказов__].
--     [Код набора]
-- GROUP BY  [Опиcания заказов__].[Код заказа])
-- ORDER BY СуммаЗаказа DESC;
-- 
--
--
-- SELECT Round(Avg([Цена]),2) AS Средняя
-- FROM
--    (SELECT DISTINCT [Опиcания заказов_].[Код набора]
--     FROM Наборы INNER JOIN ((Заказы_ INNER JOIN Покупатели_ ON Заказы_.[Код покупателя] = Покупатели_.
--     [Код покупателя])
--     INNER JOIN [Опиcания заказов_] ON Заказы_.[Код заказа] = [Опиcания заказов_].[Код заказа])
--     ON Наборы.[Код набора] = [Опиcания заказов_].[Код набора]
--     WHERE (((Покупатели_.Страна)="Белоруссия"))) НБ
-- INNER JOIN
--    (SELECT [Опиcания наборов].[Код набора], SUM([Количеcтво]*[Cтоимоcть]) AS Цена
--     FROM Фрукты INNER JOIN [Опиcания наборов] ON Фрукты.[Код фрукта] = [Опиcания наборов].[Код фрукта]
--     GROUP BY [Опиcания наборов].[Код набора]) НЦ
-- ON НБ.[Код набора] = НЦ.[Код набора];


